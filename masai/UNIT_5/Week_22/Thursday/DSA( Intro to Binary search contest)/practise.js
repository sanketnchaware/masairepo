function upperBound(arr, low, high, key) {
  while (low <= high) {
    
  }
   
}

var arr = [0, 1, 2, 6, 8, 12, 12];
var key = 12;
var low = 0;
var high = arr.length - 1;
var result = upperBound(arr, low, high, key);
console.log("result:", result);
